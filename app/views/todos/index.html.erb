<h1>Todos</h1>

<%= link_to 'New todo', new_todo_path %>

<ul class="todo">
  <% @todos.each do |todo| %>
    <li class="<%= 'completed' if todo.completed? %>"><%= todo.name %><%= button_to "Mark as complete", todo_completion_path(todo) %></li>
  <% end %>
</ul>